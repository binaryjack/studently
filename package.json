{
  "name": "studently-monorepo",
  "private": true,
  "engines": {
    "node": ">=20.x",
    "pnpm": ">=9"
  },
  "scripts": {
    "build": "pnpm -r run build",
    "test": "pnpm -r run test",
    "lint": "pnpm -r run lint",
    "typecheck": "pnpm -r run typecheck",
    "clean": "pnpm -r run clean"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^6.0.2"
  },
  "packageManager": "pnpm@9.15.0"
}
