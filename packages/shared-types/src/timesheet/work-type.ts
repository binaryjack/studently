/**
 * WorkType
 * Type of work being tracked in timesheets
 * Alias to ActivityType for domain-specific naming
 */

import { ActivityType } from "../common/activity-type";

export type WorkType = ActivityType;
