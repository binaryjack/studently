/**
 * DocumentVisibility
 * Document visibility/access level
 * Alias to VisibilityLevel for domain-specific naming
 */

import { VisibilityLevel } from "../common/VisibilityLevel";

export type DocumentVisibility = VisibilityLevel;
