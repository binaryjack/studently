/**
 * ProcessingStatus
 * Generic processing/execution status (pending, in-progress, completed, failed)
 * Used for workflow instances, tasks, processes
 */
export type ProcessingStatus = "pending" | "in-progress" | "completed" | "failed";
