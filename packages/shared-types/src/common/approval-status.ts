/**
 * ApprovalStatus
 * Generic approval/workflow status states (pending, approved, rejected)
 * Used across approval workflows, timesheets, requests, etc.
 */
export type ApprovalStatus = "pending" | "approved" | "rejected";
